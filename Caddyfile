(routes) {
    handle_path /live* {
        reverse_proxy live:5000
    }

    handle /api/robots* {
        reverse_proxy live:5000
    }

    handle {
        reverse_proxy backend:80
    }
}

doodlebot.media.mit.edu {
    import routes
}

http://doodlebot.media.mit.edu {
    import routes
}